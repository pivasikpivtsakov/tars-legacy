# Telegram
TELEGRAM_BOT_TOKEN=

# Comma-separated Telegram user IDs allowed to run /# admin commands.
# Leave empty to disable admin commands entirely.
ADMIN_USER_IDS=

# Comma-separated account (profile) IDs that act as moderators. These are the
# internal user_profiles.id values, resolved to a Telegram chat at send time.
# Moderators never receive orders; they get yes/no approval requests when users
# register or change registration data. Leave empty to disable moderation.
MODERATOR_USER_IDS=

# Postgres (consumed by both the app and docker compose)
RDS_HOSTNAME=localhost
RDS_PORT=5433
RDS_USERNAME=app
RDS_PASSWORD=app
RDS_DB_NAME=app

# Redis (aiogram FSM storage and rating stats)
REDIS_URL=redis://localhost:6379/0

# Scheduler (interval between periodic ticks, in seconds)
SCHEDULER_INTERVAL_SECONDS=30

# How long (seconds) an offered order stays takeable before it expires
OFFER_TTL_SECONDS=30

# Rating (how many recent completed orders feed the speed average)
RATING_SPEED_WINDOW=3

# Max orders a user can have pending-to-pick or in progress at once
MAX_ORDERS_PENDING=3

# Logging
LOG_LEVEL=INFO

# i18n
DEFAULT_LOCALE=en
